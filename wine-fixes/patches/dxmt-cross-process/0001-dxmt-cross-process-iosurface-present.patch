diff --git a/src/airconv/meson.build b/src/airconv/meson.build
index f5d877a..23aea2a 100644
--- a/src/airconv/meson.build
+++ b/src/airconv/meson.build
@@ -32,7 +32,8 @@ llvm_deps = [
  '-lLLVMDebugInfoDWARF', '-lLLVMObject', '-lLLVMTextAPI', '-lLLVMMCParser', 
  '-lLLVMMC', '-lLLVMDebugInfoCodeView',
  '-lLLVMBitReader', '-lLLVMCore', '-lLLVMRemarks', '-lLLVMBitstreamReader', 
- '-lLLVMBinaryFormat', '-lLLVMSupport', '-lLLVMDemangle'
+ '-lLLVMBinaryFormat', '-lLLVMSupport', '-lLLVMDemangle',
+ '/path/to/uncork/engine/zstd-1.5.6/lib/libzstd.a'
 ]
 
 airconv_args = [
diff --git a/src/d3d11/d3d11_swapchain.cpp b/src/d3d11/d3d11_swapchain.cpp
index 73c4203..a468b34 100644
--- a/src/d3d11/d3d11_swapchain.cpp
+++ b/src/d3d11/d3d11_swapchain.cpp
@@ -119,7 +119,8 @@ public:
   MTLD3D11SwapChain(
       IDXGIFactory1 *pFactory, MTLD3D11Device *pDevice, IMTLDXGIDevice *pDXGIDevice,
        HWND hWnd, const DXGI_SWAP_CHAIN_DESC1 *pDesc,
-      const DXGI_SWAP_CHAIN_FULLSCREEN_DESC *pFullscreenDesc
+      const DXGI_SWAP_CHAIN_FULLSCREEN_DESC *pFullscreenDesc,
+      bool cross_process = false
   ) :
       MTLDXGISubObject(pDevice),
       factory_(pFactory),
@@ -130,13 +131,64 @@ public:
       hWnd(hWnd),
       monitor_(wsi::getWindowMonitor(hWnd)),
       hud(WMT::DeveloperHUDProperties::instance()) {
-
+    cross_process_ = cross_process;
+
+    if (cross_process_) {
+      // Cross-process present (Uncork): the window belongs to another process
+      // (Chromium GPU proc -> browser CEF window), so there is no reachable
+      // CAMetalLayer. Create a shared IOSurface-backed texture as the present
+      // target and publish its mach port under a name keyed by the ROOT window,
+      // so the window-owning process (patched winemac.drv) can look it up and
+      // display it. See memory cef-32bit-d3dmetal-wall.
+      native_view_ = {};
+      layer_weak_ = {};
+      uint32_t w = desc_.Width ? desc_.Width : 1;
+      uint32_t h = desc_.Height ? desc_.Height : 1;
+      WMTTextureInfo ti = {};
+      ti.type = WMTTextureType2D;
+      ti.pixel_format = ConvertSwapChainFormat(desc_.Format);
+      if (ti.pixel_format == WMTPixelFormatInvalid)
+        ti.pixel_format = WMTPixelFormatBGRA8Unorm;
+      ti.usage = (WMTTextureUsage)(WMTTextureUsageRenderTarget | WMTTextureUsageShaderRead);
+      ti.options = WMTResourceStorageModeShared;
+      ti.width = w;
+      ti.height = h;
+      ti.depth = 1;
+      ti.mipmap_level_count = 1;
+      ti.sample_count = 1;
+      ti.array_length = 1;
+      ti.mach_port = 0;
+      ti.reserved = 1;  // request a GLOBAL IOSurface-backed texture (cross-process)
+      cx_shared_ = pDevice->GetMTLDevice().newSharedTexture(ti);
+      HWND root = GetAncestor(hWnd, GA_ROOT);
+      if (!root) root = hWnd;
+      uint32_t surface_id = (uint32_t)ti.mach_port;  // repurposed: IOSurface global id
+      // Publish the global id to a file the window-owning process reads. C:\windows\temp
+      // maps to <prefix>/drive_c/windows/temp in every bottle. (bootstrap_register2 mach-
+      // port sharing is sandbox-blocked on modern macOS — file + global id is reliable.)
+      if (!surface_id) {
+        ERR("cross-process present: failed to create global IOSurface texture (hwnd ", (unsigned long long)(uintptr_t)root, ")");
+      } else {
+        char path[256];
+        snprintf(path, sizeof(path), "C:\\windows\\temp\\dxmt-cxpresent-%llx.id",
+                 (unsigned long long)(uintptr_t)root);
+        FILE *f = fopen(path, "w");
+        if (f) {
+          fprintf(f, "%u %u %u\n", surface_id, w, h);
+          fclose(f);
+          WARN("cross-process present: published IOSurface id ", surface_id, " (", w, "x", h, ") -> ", path);
+        } else {
+          ERR("cross-process present: could not write id file ", path);
+        }
+      }
+    } else {
     native_view_ = WMT::CreateMetalViewFromHWND((intptr_t)hWnd, pDevice->GetMTLDevice(), layer_weak_);
 
     if (!native_view_) {
       ERR("Failed to create metal view, it seems like your Wine has no exported symbols needed by DXMT.");
       abort();
     }
+    }
 
     if constexpr (EnableMetalFX) {
       scale_factor = std::max(Config::getInstance().getOption<float>("d3d11.metalSpatialUpscaleFactor", 2), 1.0f);
@@ -146,6 +198,10 @@ public:
                                  pDevice->GetDXMTDevice().queue().cmd_library,
                                  scale_factor, desc_.SampleDesc.Count));
 
+    if (cross_process_)
+      presenter->setCrossProcessTarget(cx_shared_, desc_.Width ? desc_.Width : 1,
+                                       desc_.Height ? desc_.Height : 1);
+
     frame_latency = kSwapchainLatency;
     present_semaphore_ = CreateSemaphore(nullptr, frame_latency,
                                          DXGI_MAX_SWAP_CHAIN_BUFFERS, nullptr);
@@ -206,7 +262,8 @@ public:
 
   ~MTLD3D11SwapChain() {
     device_context_->WaitUntilGPUIdle();
-    WMT::ReleaseMetalView(native_view_);
+    if (native_view_)  // null in cross-process mode (no CAMetalLayer view)
+      WMT::ReleaseMetalView(native_view_);
     native_view_ = {};
     CloseHandle(present_semaphore_);
   };
@@ -1048,6 +1105,8 @@ public:
 
 private:
   bool LayerSupportEDR() {
+    if (!layer_weak_)  // cross-process: no layer, assume SDR
+      return false;
     WMTEDRValue edr_value;
     MetalLayer_getEDRValue(layer_weak_, &edr_value);
     return edr_value.maximum_potential_edr_color_component_value > 1.0f;
@@ -1057,6 +1116,9 @@ private:
   Com<IMTLDXGIDevice> dxgi_device_;
   WMT::Object native_view_;
   WMT::MetalLayer layer_weak_;
+  bool cross_process_ = false;
+  WMT::Reference<WMT::Texture> cx_shared_;  // cross-process shared present target
+  char cx_name_[64] = {};                   // bootstrap name for the shared target
   ULONG presentation_count_;
   DXGI_SWAP_CHAIN_DESC1 desc_;
   DXGI_SWAP_CHAIN_FULLSCREEN_DESC fullscreen_desc_;
@@ -1096,12 +1158,18 @@ CreateSwapChain(
     return DXGI_ERROR_INVALID_CALL;
   InitReturnPtr(ppSwapChain);
 
-  DWORD window_process_id;
+  // Cross-process present (Uncork): when the window belongs to another process
+  // (Chromium's GPU process presenting into the browser process's CEF window),
+  // there is no reachable CAMetalLayer. The swapchain then renders into a shared
+  // IOSurface-backed texture whose mach port is published for the window-owning
+  // process (patched winemac.drv) to look up and display. See memory
+  // cef-32bit-d3dmetal-wall.
+  DWORD window_process_id = 0;
   GetWindowThreadProcessId(hWnd, &window_process_id);
-  if (GetProcessId(GetCurrentProcess()) != window_process_id) {
-    ERR("CreateSwapChain: cross-process swapchain not supported yet");
-    return E_FAIL;
-  }
+  bool cross_process = window_process_id && (GetProcessId(GetCurrentProcess()) != window_process_id);
+  if (cross_process)
+    WARN("CreateSwapChain: cross-process swapchain (window pid ", window_process_id,
+         " != our pid ", GetProcessId(GetCurrentProcess()), ") — using shared-texture present");
 
   Com<IMTLDXGIDevice> layer_factory;
   if (FAILED(pDevice->QueryInterface(IID_PPV_ARGS(&layer_factory)))) {
@@ -1113,7 +1181,7 @@ CreateSwapChain(
       pDesc->BufferCount != 1) {
     WARN("CreateSwapChain: unsupported swap effect ", pDesc->SwapEffect, " with backbuffer size ", pDesc->BufferCount);
   }
-  if (env::getEnvVar("DXMT_METALFX_SPATIAL_SWAPCHAIN") == "1") {
+  if (!cross_process && env::getEnvVar("DXMT_METALFX_SPATIAL_SWAPCHAIN") == "1") {
     if (pDevice->GetMTLDevice().supportsFXSpatialScaler()) {
       *ppSwapChain = new MTLD3D11SwapChain<true>(
           pFactory, pDevice, layer_factory.ptr(), hWnd, pDesc, pFullscreenDesc
@@ -1124,7 +1192,7 @@ CreateSwapChain(
     }
   }
   *ppSwapChain = new MTLD3D11SwapChain<false>(
-      pFactory, pDevice, layer_factory.ptr(), hWnd, pDesc, pFullscreenDesc
+      pFactory, pDevice, layer_factory.ptr(), hWnd, pDesc, pFullscreenDesc, cross_process
   );
   return S_OK;
 };
diff --git a/src/dxmt/dxmt_context.cpp b/src/dxmt/dxmt_context.cpp
index 3e11ed6..2ea7904 100644
--- a/src/dxmt/dxmt_context.cpp
+++ b/src/dxmt/dxmt_context.cpp
@@ -1028,10 +1028,14 @@ ArgumentEncodingContext::flushCommands(WMT::CommandBuffer cmdbuf, uint64_t seqId
       );
       auto t1 = clock::now();
       currentFrameStatistics().drawable_blocking_interval += (t1 - t0);
-      if (data->after > 0)
-        cmdbuf.presentDrawableAfterMinimumDuration(drawable, data->after);
-      else
-        cmdbuf.presentDrawable(drawable);
+      // Cross-process present (Uncork): encodeCommands rendered into a shared
+      // texture and returned a null drawable — nothing to presentDrawable().
+      if (drawable) {
+        if (data->after > 0)
+          cmdbuf.presentDrawableAfterMinimumDuration(drawable, data->after);
+        else
+          cmdbuf.presentDrawable(drawable);
+      }
       data->~PresentData();
       break;
     }
diff --git a/src/dxmt/dxmt_presenter.cpp b/src/dxmt/dxmt_presenter.cpp
index 87624a8..66948f5 100644
--- a/src/dxmt/dxmt_presenter.cpp
+++ b/src/dxmt/dxmt_presenter.cpp
@@ -13,12 +13,19 @@ Presenter::Presenter(WMT::Device device, WMT::MetalLayer layer, InternalCommandL
     layer_(layer),
     lib_(lib),
     sample_count_(sample_count) {
-  layer_.getProps(layer_props_);
+  if (layer_) {
+    layer_.getProps(layer_props_);
+    layer_props_.contents_scale = layer_props_.contents_scale * scale_factor;
+  } else {
+    // Cross-process present (Uncork): no CAMetalLayer to query — the render target
+    // is a shared texture set later via setCrossProcessTarget(). Zero-init props.
+    layer_props_ = {};
+    layer_props_.contents_scale = scale_factor > 0 ? scale_factor : 1.0;
+  }
   layer_props_.device = device;
   layer_props_.opaque = true;
   layer_props_.display_sync_enabled = false;
   layer_props_.framebuffer_only = false; // how strangely setting it true results in worse performance
-  layer_props_.contents_scale = layer_props_.contents_scale * scale_factor;
 
   WMTTextureInfo texture_info;
   texture_info.type = WMTTextureType2D;
@@ -49,7 +56,7 @@ Presenter::changeLayerProperties(
   colorspace_ = colorspace;
   if (should_invalidated)
     pso_valid.clear(); // defer changes
-  else
+  else if (layer_)  // cross-process: no layer
     layer_.setProps(layer_props_);
   return should_invalidated;
 }
@@ -105,9 +112,10 @@ Presenter::PresentState
 Presenter::synchronizeLayerProperties() {
   uint64_t display_setting_version = 0;
 
-  WMTQueryDisplaySettingForLayer(
-      layer_.handle, &display_setting_version, &display_colorspace_, &display_hdr_metadata_, &display_edr_value_
-  );
+  if (layer_)  // cross-process: no layer to query display settings for
+    WMTQueryDisplaySettingForLayer(
+        layer_.handle, &display_setting_version, &display_colorspace_, &display_hdr_metadata_, &display_edr_value_
+    );
 
   if (display_setting_version != display_setting_version_) {
     display_setting_version_ = display_setting_version;
@@ -122,8 +130,10 @@ Presenter::synchronizeLayerProperties() {
   if (unlikely(!pso_valid.test_and_set())) {
     frame_presented_.wait(frame_requested_);
     buildRenderPipelineState(final_colorspace == WMTColorSpaceHDR_PQ, is_hdr && hdr_metadata != nullptr, sample_count_ > 1, gamma_version_ != 0);
-    layer_.setProps(layer_props_);
-    layer_.setColorSpace(final_colorspace);
+    if (layer_) {  // cross-process: no layer props/colorspace to push
+      layer_.setProps(layer_props_);
+      layer_.setColorSpace(final_colorspace);
+    }
   }
 
   DXMTPresentMetadata metadata;
@@ -156,13 +166,19 @@ Presenter::encodeCommands(
     std::function<void(WMT::RenderCommandEncoder)> &&wait_fences,
     std::function<void(WMT::RenderCommandEncoder)> &&update_fences
 ) {
-  auto drawable = layer_.nextDrawable();
-
+  // Cross-process (Uncork): present into the shared IOSurface-backed texture the
+  // window-owning process displays; there is no drawable to presentDrawable().
+  WMT::MetalDrawable drawable{};
   WMTRenderPassInfo info;
   WMT::InitializeRenderPassInfo(info);
   info.colors[0].load_action = WMTLoadActionDontCare;
   info.colors[0].store_action = WMTStoreActionStore;
-  info.colors[0].texture = drawable.texture();
+  if (cross_process_) {
+    info.colors[0].texture = cx_target_.handle;
+  } else {
+    drawable = layer_.nextDrawable();
+    info.colors[0].texture = drawable.texture();
+  }
   auto encoder = cmdbuf.renderCommandEncoder(info);
   wait_fences(encoder);
   encoder.setFragmentTexture(backbuffer, 0);
diff --git a/src/dxmt/dxmt_presenter.hpp b/src/dxmt/dxmt_presenter.hpp
index 77eb35c..ccb68d3 100644
--- a/src/dxmt/dxmt_presenter.hpp
+++ b/src/dxmt/dxmt_presenter.hpp
@@ -71,6 +71,22 @@ public:
       std::function<void(WMT::RenderCommandEncoder)> &&update_fences
   );
 
+  /* Cross-process present (Uncork): when a swapchain is created for a window owned
+   * by another process (e.g. Chromium's GPU process presenting into the browser
+   * process's CEF window), there is no CAMetalLayer we can reach. Instead we present
+   * into a shared IOSurface-backed texture that the window-owning process (winemac.drv)
+   * looks up by mach port and displays. In that mode encodeCommands() blits into
+   * `cx_target_` and returns a null drawable (nothing to presentDrawable). */
+  void setCrossProcessTarget(WMT::Reference<WMT::Texture> target, uint32_t width, uint32_t height) {
+    cx_target_ = std::move(target);
+    cx_width_ = width;
+    cx_height_ = height;
+    cross_process_ = true;
+    layer_props_.drawable_width = width;
+    layer_props_.drawable_height = height;
+  }
+  bool isCrossProcess() const { return cross_process_; }
+
 private:
   void buildRenderPipelineState(bool is_pq, bool with_hdr_metadata, bool is_ms, bool gamma_enable);
 
@@ -95,5 +111,9 @@ private:
   std::atomic_flag pso_valid = 0;
   uint64_t frame_requested_ = 0;
   CpuFence frame_presented_ = 0;
+  bool cross_process_ = false;
+  WMT::Reference<WMT::Texture> cx_target_;
+  uint32_t cx_width_ = 0;
+  uint32_t cx_height_ = 0;
 };
 } // namespace dxmt
\ No newline at end of file
diff --git a/src/util/com/com_guid.cpp b/src/util/com/com_guid.cpp
index 11bf4a7..05bda6c 100644
--- a/src/util/com/com_guid.cpp
+++ b/src/util/com/com_guid.cpp
@@ -8,6 +8,7 @@
  * See <https://github.com/doitsujin/dxvk/blob/master/LICENSE>
  */
 
+#include <iomanip>
 #include <mutex>
 #include <unordered_set>
 
diff --git a/src/winemetal/unix/meson.build b/src/winemetal/unix/meson.build
index 3f20c2b..4c54ee8 100644
--- a/src/winemetal/unix/meson.build
+++ b/src/winemetal/unix/meson.build
@@ -20,6 +20,7 @@ winemetal_unix_ld_args = [
   '-framework', 'MetalFX', '-weak_framework', 'Foundation',
   '-weak_framework', 'CoreGraphics', '-weak_framework', 'QuartzCore',
   '-weak_framework', 'ColorSync',  '-weak_framework', 'Cocoa',
+  '-framework', 'IOSurface',
   '-lsqlite3',
 ]
 winemetal_unix_link_depends = []
diff --git a/src/winemetal/unix/winemetal_unix.c b/src/winemetal/unix/winemetal_unix.c
index 53a0341..d49bef4 100644
--- a/src/winemetal/unix/winemetal_unix.c
+++ b/src/winemetal/unix/winemetal_unix.c
@@ -7,6 +7,7 @@
 #import <CoreFoundation/CFRunLoop.h>
 #import <Metal/Metal.h>
 #import <MetalFX/MetalFX.h>
+#import <IOSurface/IOSurface.h>
 #import <QuartzCore/QuartzCore.h>
 #include "objc/objc-runtime.h"
 #include <bootstrap.h>
@@ -2700,6 +2701,44 @@ _MTLDevice_newSharedTexture(void *obj) {
   id<MTLDevice> device = (id<MTLDevice>)params->device;
   struct WMTTextureInfo *info = params->info.ptr;
 
+  /* Uncork cross-process present: reserved==1 requests a GLOBAL IOSurface-backed
+   * texture (Metal's own shared-texture IOSurfaces are not global, so they can't
+   * be found via IOSurfaceLookup across processes; and bootstrap_register2 mach-
+   * port sharing is sandbox-blocked on modern macOS). We create the IOSurface
+   * ourselves with kIOSurfaceIsGlobal and return its global ID (uint32) in
+   * info->mach_port so the window-owning process can IOSurfaceLookup() it. */
+  if (info->reserved == 1 && !info->mach_port) {
+    size_t bpe = 4; /* BGRA8 */
+    size_t width = info->width ? info->width : 1;
+    size_t height = info->height ? info->height : 1;
+    size_t bpr = IOSurfaceAlignProperty(kIOSurfaceBytesPerRow, width * bpe);
+    NSDictionary *props = @{
+      (id)kIOSurfaceWidth: @(width),
+      (id)kIOSurfaceHeight: @(height),
+      (id)kIOSurfaceBytesPerElement: @(bpe),
+      (id)kIOSurfaceBytesPerRow: @(bpr),
+      (id)kIOSurfacePixelFormat: @((uint32_t)0x42475241 /* 'BGRA' */),
+      (id)kIOSurfaceIsGlobal: @YES,
+    };
+    IOSurfaceRef surface = IOSurfaceCreate((CFDictionaryRef)props);
+    if (!surface) {
+      params->ret = 0;
+      return STATUS_SUCCESS;
+    }
+    MTLTextureDescriptor *desc = [[MTLTextureDescriptor alloc] init];
+    fill_texture_descriptor(desc, info);
+    desc.storageMode = MTLStorageModeShared;
+    id<MTLTexture> ret = [device newTextureWithDescriptor:desc iosurface:surface plane:0];
+    params->ret = (obj_handle_t)ret;
+    if (ret) {
+      info->gpu_resource_id = [ret gpuResourceID]._impl;
+      info->mach_port = IOSurfaceGetID(surface); /* GLOBAL id, NOT a mach port */
+    }
+    CFRelease(surface); /* the texture retains it */
+    [desc release];
+    return STATUS_SUCCESS;
+  }
+
   if (info->mach_port) {
     MTLSharedTextureHandle *handle = [[MTLSharedTextureHandle alloc] initWithMachPort:info->mach_port];
     id<MTLTexture> ret = [device newSharedTextureWithHandle:handle];
